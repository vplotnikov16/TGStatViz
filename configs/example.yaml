title: "Пример раскадровки"
description: "Проверка базовой загрузки"

defaults:
  style: "dark"

slides:
  - metric: "personal.cumulative_messages"
    renderer: "animated_line_chart"
    duration: 10
